name = "Farhan"

print("Hello " + name) # concatenation
print(f"Hello {name}") # f-string, allows us to embed expressions inside string literals, using curly braces {}
print(name*2) # repetition, repeats the string a specified number of times
print(name+name) # concatenation, combines two strings into one, the + operator is used to concatenate strings