def divide(dividend, divisor):
    if divisor != 0:
        print(dividend / divisor)
    else:
        print("Error: Division by zero is not allowed.")

divide(10, 2)  # arguments 10 and 2 are passed to parameters dividend and divisor respectively